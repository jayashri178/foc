#include<stdio.h>
#include<conio.h>
#include<math.h>
int main()
{
	
	int a,b,c;
	float s,d,area;
	printf("enter the sides of the triangle:");
	scanf("%d%d%d",&a,&b,&c);
	s=a+b+c/2;
	d=(s*(s-a)*(s-b)*(s-c));
	area=sqrt(d);
	printf("the area of the triangle is %f\n",area);
	getch();
	return(0);
  }
